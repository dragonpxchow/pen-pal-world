import { Typography } from "@material-ui/core";

const UserProfileAccountOption = () => {
  return <Typography variant="h5">User Profile Account Options</Typography>;
};

export default UserProfileAccountOption;
